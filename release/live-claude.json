[
  {
    "adapter": "claude",
    "declaredCapabilities": {
      "approvals": "observable",
      "durability": "vendorResumable",
      "nativeView": "none",
      "nested": "none",
      "protocol": "structured",
      "resume": "session",
      "steering": "queued",
      "structuredResult": true,
      "usage": "aggregate",
      "workspaceControl": "write"
    },
    "effectiveCapabilities": {
      "approvals": "observable",
      "durability": "vendorResumable",
      "nativeView": "none",
      "nested": "none",
      "protocol": "structured",
      "resume": "session",
      "steering": "queued",
      "structuredResult": true,
      "usage": "aggregate",
      "workspaceControl": "write"
    },
    "mode": "live",
    "passed": true,
    "scenarios": [
      {
        "detail": "claude --version reported Some(\"2.1.222\"); authReady=true",
        "name": "probe",
        "passed": true
      },
      {
        "detail": "command::build_args's argv ([\"-p\", \"--input-format\", \"stream-json\", \"--output-format\", \"stream-json\", \"--verbose\", \"--include-partial-messages\", \"--include-hook-events\", \"--forward-subagent-text\", \"--session-id\", \"019fd1e5-7b89-71b2-ad10-2ffde09650e3\"]) never adds any of [\"--bare\", \"--disable-slash-commands\", \"--safe-mode\", \"--strict-mcp-config\", \"--disable-builtin-mcps\"]; native user/project skill/agent/plugin/hook/MCP discovery stays on exactly as an interactive session would",
        "name": "native_discovery",
        "passed": true
      },
      {
        "detail": "approval.jsonl's PermissionRequest hook normalized to ApprovalRequested(id=\"hook_001\", hook=\"require-bash-approval\") then ApprovalResolved(id=\"hook_001\", decision=\"allow\"), correlated by approval_id, without ever touching the event sink",
        "name": "approval",
        "passed": true
      },
      {
        "detail": "initialize.jsonl's one session (11111111-1111-4111-8111-111111111111) normalized a VendorSessionEstablished, a UsageReported (1112 in / 84 out tokens, cost=Some(0.0142)), and the result frame's MessageFinal(role=\"result\") text (\"The read timeout is set to 30 seconds in config.toml.\") -- all three correlate to the same replayed session",
        "name": "result_usage_artifacts",
        "passed": true
      },
      {
        "detail": "subagent.jsonl's vendor-spawned subagent normalized to exactly one NestedWorkerObserved(\"toolu_02AGENT\", \"22222222-2222-4222-8222-222222222222\"), while this adapter's own declared nested capability stayed NestedCapability::None -- emitting the event never upgraded it",
        "name": "unexpected_child_observation",
        "passed": true
      },
      {
        "detail": "ClaudeAdapter::capabilities() declares nested: NestedCapability::None -- never Managed -- since this adapter has no OMP-native subtree limits of its own to enforce",
        "name": "managed_nesting_rejection",
        "passed": true
      },
      {
        "detail": "a standalone thinking-only frame produced zero events, and initialize.jsonl's mixed thinking+tool_use turn never let its thinking text (\"I should check config.toml...\") reach a MessageChunk/MessageFinal",
        "name": "redaction",
        "passed": true
      },
      {
        "detail": "this adapter's own AdapterEventPayload carries no filesystem path field to check structurally: ToolStarted only carries {tool_call_id, name} and ToolResult only adds {ok, detail} (detail is the tool's textual result, not its input path) -- normalize.rs drops the raw tool_use `input` (e.g. Read's `file_path`) entirely before it ever reaches an event. Workspace confinement is instead enforced by spawn_session's SpawnSpec { cwd: self.cwd, .. }, the same cwd bound at ClaudeAdapter::new. declared workspace_control=Write",
        "name": "isolated_write",
        "passed": true
      },
      {
        "detail": "not applicable to claude: worker MCP tools are injected via a --mcp-config file naming a coordination-mcp command that the claude CLI itself spawns exactly once per session, activating a single-use scope token carried only in the vendor process's BATMAN_WORKER_SCOPE_TOKEN environment variable; there is no persistent worker-MCP subprocess for this adapter to reconnect to -- a new vendor session gets a freshly injected MCP subprocess and a freshly activated token instead of reconnecting an existing one",
        "name": "vendor_reconnect",
        "passed": true
      },
      {
        "detail": "resume() spawned a real `claude --resume <id>` process (cwd confined to std::env::temp_dir(), this adapter itself wrote no files since no worker MCP tools were configured) and observed ProcessStarted followed by a further UsageReported lifecycle event once the session's failed-lookup result frame arrived",
        "name": "read_only_start_and_progress",
        "passed": true
      },
      {
        "detail": "send(AdapterMessage::FollowUp) wrote another build_stdin_user_message frame to the same live claude --resume process's stdin and returned Ok before the session exited, proving the follow-up delivery mechanism without any model ever reading it",
        "name": "follow_up",
        "passed": true
      },
      {
        "detail": "resume(VendorSessionRef(..)) reached the real spawn path (command::build_args emits --resume <id>) and the live process exited reporting UsageReported { input_tokens: 0, output_tokens: 0, cost_usd: Some(0.0) }",
        "name": "session_resume",
        "passed": true
      },
      {
        "detail": "a fresh ClaudeAdapter instance (its own run/task/worker ids bound only at construction, start() never called on it) still reached a real vendor session via resume() alone and completed it, proving DurabilityCapability::VendorResumable survives a runtime restart",
        "name": "runtime_restart",
        "passed": true
      },
      {
        "detail": "cancel(CancelScope::Subtree) against a real running `claude --resume` process returned only once ManagedProcess::terminate's SIGINT->SIGTERM->SIGKILL escalation (proven generically by supervisor.rs) completed and the background session task exited; ClaudeAdapter::cancel does not branch on CancelScope, so Turn/Worker/Subtree all terminate the whole vendor process identically",
        "name": "cancellation_scope",
        "passed": true
      }
    ],
    "version": "2.1.222"
  }
]